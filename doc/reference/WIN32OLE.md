# WIN32OLE

擬似記述マニュアル

OLE を操作できるオブジェクトを生成します。
OLE が IDispatch で持つメソッドを直接呼び出すことができます。

## メンバー一覧

### コンストラクタ

- [WIN32OLE](#win32ole)

### メソッド

- [invoke](#invoke)
- [set](#set)
- [get](#get)
- [getConstant](#getconstant)
- [addEvent](#addevent)

---

### WIN32OLE

コンストラクタ

**引数**

| 引数 | 既定値 | 説明 |
| --- | --- | --- |
| `name` | `&nbsp;` |  |

**解説**

コンストラクタ

---

### invoke

メソッド

**引数**

| 引数 | 既定値 | 説明 |
| --- | --- | --- |
| `name` | `&nbsp;` | メソッド名 |
| `...` | `&nbsp;` | 以下パラメータ |

**解説**

メソッドの実行

---

### set

メソッド

**引数**

| 引数 | 既定値 | 説明 |
| --- | --- | --- |
| `name` | `&nbsp;` | プロパティ名 |
| `value` | `&nbsp;` |  |

**解説**

プロパティの設定

---

### get

メソッド

**引数**

| 引数 | 既定値 | 説明 |
| --- | --- | --- |
| `name` | `&nbsp;` | プロパティ名 |

**戻り値**

設定値

**解説**

プロパティの取得

---

### getConstant

メソッド

**引数**

| 引数 | 既定値 | 説明 |
| --- | --- | --- |
| `obj` | `this` | 指定したオブジェクトのプロパティとして定数を取得します。<br>省略した場合は自分のオブジェクト内部に取得します。 |

**解説**

定数の取得

---

### addEvent

メソッド

**引数**

| 引数 | 既定値 | 説明 |
| --- | --- | --- |
| `eventName` | `&nbsp;` | イベント種別名 (例: "DWebBrowser2Events2" ) |
| `receiver` | `&nbsp;` | イベントをうけるオブジェクト<br>オブジェクトが持つイベント名と同じ名前のメソッドが呼び出されます。<br>該当するメソッドが無い場合はなにも処理されません。 |

**解説**

イベント受信オブジェクトの登録

---
